# SwingTrade Pro — core runtime.
# Installed into an isolated venv by run.sh / run.command (see README).
streamlit>=1.30
streamlit-option-menu>=0.3.6  # icon-based sidebar nav (graceful fallback to st.radio if absent)
yfinance>=0.2.50
pandas>=2.0
numpy>=1.24
plotly>=5.18
scikit-learn>=1.3
certifi>=2024.2.2
youtube-transcript-api>=0.6  # YouTube page transcripts (optional; falls back to title+description)
